Create a Deployment named api-deploy using image httpd:2.4.

Intentionally misconfigure the selector once and observe Pod behavior.

Fix the selector so Pods are created correctly.

Confirm the Deployment is healthy.
