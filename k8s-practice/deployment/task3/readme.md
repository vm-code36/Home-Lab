Create a Deployment named payment-service with image nginx:1.24 and 5 replicas.

Update the image to nginx:1.26 using a rolling update.

Monitor the rollout status live.

Verify that Pods are replaced gradually, not all at once.
