TASK SET 1 — Basic Deployment Control

Create a Deployment named web-app using image nginx:1.25 with 3 replicas.

Ensure all Pods have label tier=frontend.

Verify that the Deployment creates the correct number of Pods.

Scale the Deployment down to 1 replica, then back to 4 replicas.

Delete only one Pod manually and observe what happens.
